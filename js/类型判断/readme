#typeof
1.可以判断除null之外的所有原始类型
2.除了function其他所有的引用类型都会被判断成object
3.typeof是通过将值转换成为二进制后判断判断二进制前三位是否为0,否则为object

#instanceof
1.只能判断引用类型
2.通过原型链的查找来判断类型

#Object.prototype.toString('this')

将o作为ToObject(this)的执行结果
定义一个class作为内部属性[[class]]的值
返回由"[object"和class 和"]"组成的字符串

Object.prototype.toString(a)
ToObject(a)
#Object.prototype.toString.call(XXX)

#Array.isArray(x)